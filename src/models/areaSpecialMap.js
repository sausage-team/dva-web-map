import { getHotMap } from '../services/user';

export default {
  namespace: 'areaSpecialMap',
  state: {},
  reducers: {},
  effects: {
    // *getAreaSpecia({ payload }, { call, put, select }) {
    //   let AreaMap = new AreaMap()
    //  yield call()
    // },
  },
  subscriptions: {},
};
